import React from "react"

export const SidebarHeader = ({text}) => {
    return (
        <div className="widget-header-2 position-relative">
            <h5 className="mt-5 mb-15">{text}</h5>
        </div>
    )
}
